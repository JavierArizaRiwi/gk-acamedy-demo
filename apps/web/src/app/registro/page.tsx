'use client';import{useState}from'react';import{useRouter}from'next/navigation';import{api}from'@/lib/api';export default function Register(){const[f,setF]=useState({name:'',email:'',phone:'',password:''});const[msg,setMsg]=useState('');const r=useRouter();async function submit(e:any){e.preventDefault();try{const d=await api('/auth/register',{method:'POST',body:JSON.stringify(f)});localStorage.setItem('token',d.accessToken);localStorage.setItem('user',JSON.stringify(d.user));r.push('/dashboard')}catch(e:any){setMsg(e.message)}}return<div className="authWrap"><form className="authCard form" onSubmit={submit}><div className="kicker">Comienza hoy</div><h2>Crear cuenta</h2>{msg&&<p>{msg}</p>}{[['name','Nombre'],['email','Correo'],['phone','Celular'],['password','Contraseña']].map(([k,l])=><div className="field" key={k}><label>{l}</label><input type={k==='password'?'password':k==='email'?'email':'text'} value={(f as any)[k]} onChange={e=>setF({...f,[k]:e.target.value})} required={k!=='phone'}/></div>)}<button className="btn">Registrarme</button></form></div>}
