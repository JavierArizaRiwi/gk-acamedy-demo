import { Injectable, NotFoundException } from '@nestjs/common'; import { InjectModel } from '@nestjs/mongoose'; import { Model } from 'mongoose'; import { Plan,PlanDocument } from './plan.schema';
@Injectable() export class PlansService { constructor(@InjectModel(Plan.name) private model:Model<PlanDocument>){} list(activeOnly=true){return this.model.find(activeOnly?{active:true}:{}).sort({price:1}).lean();} async get(id:string){const p=await this.model.findById(id);if(!p)throw new NotFoundException('Plan no encontrado');return p;} create(data:any){return this.model.create(data);} async update(id:string,data:any){const p=await this.model.findByIdAndUpdate(id,data,{new:true});if(!p)throw new NotFoundException('Plan no encontrado');return p;} }
